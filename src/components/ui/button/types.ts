export enum ButtonType {
  AddNew,
  Save,
  Delete,
  Edit,
  Update,
  Print,
  Preview,
  Config,
  TrashRestore,
  ViewLog,
  CloseModal,
  OkModal,
  CancelModal,
  Apply,
  Custom,
}

export enum ButtonPressed {
  OK,
  Close,
  Cancel,
}

export enum ButtonId {
  AddNew = 'btnAddNew',
  Save = 'btnSave',
  Delete = 'btnDelete',
  Edit = 'btnEdit',
  Update = 'btnUpdate',
  Print = 'btnPrint',
  Preview = 'btnPreview',
  Config = 'btnConfig',
  TrashRestore = 'btnTrashRestore',
  ViewLog = 'btnViewLog',
  Apply = 'btnApply',
}
